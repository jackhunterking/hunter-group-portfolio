# X

Account: x.com/jackvetara

## Output

Return the post or thread in a code block.

## Structure

Hook first. The first post must stand alone.

Write one post when possible. Use an optional 2 to 4 post thread only when the idea needs it.

## Rules

- 280 characters maximum per post.
- 0 to 2 hashtags only if they fit.
- No CTA.
- Use Turkish except accepted English real estate terms.
- No dashes in generated copy.
- When staging in X, paste Turkish text from the clipboard instead of typing it character by character.
- Before publishing, visually confirm that Turkish letters such as `ç`, `ğ`, `ı`, `İ`, `ö`, `ş`, and `ü` still display correctly in the live draft.
